#!/usr/bin/node
// A script that prints 3 line of string using loop
const language = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;
while (language[i]) {
  console.log(language[i]);
  i++;
}
